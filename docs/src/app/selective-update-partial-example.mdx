```tsx
const user = Signal({
  name: "Astrid",
  age: 30,
})

function Name() {
  // 🚀 re-renders only when the name changes
  const name = useComputed((monitor) => user.read(monitor).name)

  console.log("Name render")

  return <span>{name}</span>
}

user.write((prev) => ({ ...prev, age: 31 }))
// Console: (nothing) - Name stays silent
```
